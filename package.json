{
  "name": "pumproom-sdk",
  "version": "0.1.0",
  "main": "dist/bundles/pumproom-sdk-latest.umd.js",
  "module": "dist/bundles/pumproom-sdk-latest.esm.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "dev": "vite",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "@vitest/coverage-istanbul": "^3.2.4",
    "jsdom": "^26.1.0",
    "typescript": "^5.2.2",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  }
}
