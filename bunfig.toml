[install]
# Use the exact versions specified in package.json
exact = false

# Enable peer dependency auto-installation
peer = true

# Cache dependencies for faster installation
cache = true

minimumReleaseAge = 432000
